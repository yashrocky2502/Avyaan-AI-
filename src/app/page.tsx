'use client';

import React, { useEffect } from "react";

/**
 * MarketPulse Entry Point.
 * Standardizes the terminal experience by rendering the state-driven AppLayout.
 * Telegram initialization is handled globally in AppLayout to prevent hydration freezing.
 */
export default function Home() {
  return null; // All content is handled by the persistent AppLayout container logic
}
